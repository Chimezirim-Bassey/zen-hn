name: zen-hn

services:
  zen-web:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    environment:
      POSTGRES_DB_HOST: zen-db
      REDIS_HOST: zen-redis
      CACHE_ENGINE: redis
      EMAIL_BACKEND: user_account.email_backend.CeleryEmailBackend
      RUN_MIGRATIONS: 0
      RUN_COLLECTSTATIC: 0
    command: gunicorn zen_hn.wsgi:application --bind 0.0.0.0:8000
    volumes:
      - static_files:/zen_hn/staticfiles
      - media_files:/zen_hn/media
      - .:/zen_hn
    ports:
      - "8000:8000"
    depends_on:
      - zen-db
      - zen-redis
    container_name: zen-web

  zen-db:
    image: postgres
    env_file:
      - .env
    volumes:
      - db_data:/var/lib/postgresql/data
    container_name: zen-db
    restart: always
    ports:
      - "5432:5432"

  zen-celery:
    build:
      context: .
    env_file:
      - .env
    environment:
      REDIS_HOST: redis
    command: python -m celery -A zen_hn worker --loglevel=info
    depends_on:
      - zen-web
      - zen-redis
    container_name: zen-celery
    restart: always

  zen-redis:
    image: redis:alpine
    container_name: zen-redis
    volumes:
      - redis_data:/data
    restart: always

  zen-nginx:
    image: nginx:latest
    ports:
      - "8001:80"
    volumes:
      - ./nginx/default.conf:/etc/nginx/nginx.conf
      - static_files:/zen_hn/staticfiles
      - media_files:/zen_hn/media
    depends_on:
      - zen-web
    container_name: zen-nginx

volumes:
  static_files:
    name: static_files
  media_files:
    name: media_files
  db_data:
    name: db_data
  redis_data:
    name: redis_data
